{
  "name": "ez-minutes-suite-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only regression test suite for the EZ Minutes suite. Not part of the distributed app itself - Capture, Compile, and Tunebook Editor remain plain static HTML files with no build step or dependencies of their own. This folder tests them from the outside using Node's built-in test runner and jsdom.",
  "scripts": {
    "test": "node --test \"*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "papaparse": "^5.5.4"
  }
}
